npm --registry https://registry.npm.taobao.org install --global --production windows-build-tools
npm --registry https://registry.npm.taobao.org install -g node-sass

1.需要一个link 对应关系表，以及对应的url拼接规则--比如在header的应处理任务部分 对应 index/header/LoginInfo.jsx/<Popover placement="bottom"


明天
1.开始各个页面的初始化操作
2.现在关联到 1> shortcut 的处理，最好清理掉，而不是变换，因为shortcut可以做刷新

